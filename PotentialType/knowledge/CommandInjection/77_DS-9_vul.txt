This function performs a DNS query for a specified domain and query type using a command-line tool, and returns the result. However, it is vulnerable to command injection due to the way it constructs the command.