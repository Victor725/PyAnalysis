The function checks for running processes that match a given name and returns a list of details about these processes. It is vulnerable to command injection due to its use of shell execution with unsanitized input.